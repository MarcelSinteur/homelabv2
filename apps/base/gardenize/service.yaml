apiVersion: v1
kind: Service
metadata:
  name: svc-gardenize
  namespace: gardenize
  labels:
    app: gardenize
  annotations:
spec:
  type: LoadBalancer
  ports:
  - port: 10010
    targetPort: 8080
  selector:
    app: gardenize